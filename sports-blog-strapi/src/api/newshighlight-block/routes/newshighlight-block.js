'use strict';

/**
 * newshighlight-block router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::newshighlight-block.newshighlight-block');
