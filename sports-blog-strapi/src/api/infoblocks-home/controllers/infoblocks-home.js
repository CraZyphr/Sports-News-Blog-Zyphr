'use strict';

/**
 * infoblocks-home controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::infoblocks-home.infoblocks-home');
