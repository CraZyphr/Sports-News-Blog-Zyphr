'use strict';

/**
 * infoblocks-new controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::infoblocks-new.infoblocks-new');
